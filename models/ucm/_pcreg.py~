from sklearn.decomposition import PCA

class PCRegMixin(PCA):

    def fit_principal_component_regression(self, X, y):

        if "n_components" in self.get_params():
            n_components = self.n_components

        
